# Autonomous-Emergency-Braking
The AEB system is a safety feature that automatically applies brakes to prevent or mitigate collisions  when the driver fails to react. It uses sensor fusion (radar, camera, LiDAR) to detect obstacles,  calculates collision risk, and triggers braking if necessary. 
